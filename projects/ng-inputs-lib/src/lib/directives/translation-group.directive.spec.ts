import { TranslationGroupDirective } from './translation-group.directive';

describe('TranslationGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new TranslationGroupDirective();
    expect(directive).toBeTruthy();
  });
});
